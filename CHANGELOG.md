# Changelog

## Unreleased – Initial clean base (branch dev)
- Nettoyage complet du dépôt (artefacts, secrets, dépendances inutiles).
- Refonte du frontend Vite avec animation CyberMaze optimisée et améliorations a11y.
- Backend FastAPI rendu optionnel avec fallback en mémoire et configuration via `.env`.
- Nouvelle chaîne Docker multi-stage (Node → Caddy) prête pour Traefik v3.
- Documentation de démarrage, `.env.example`, `.gitignore/.dockerignore` homogènes.
